apiVersion: apps/v1
kind: Deployment
metadata:
  name: pagerank-queuer 
  labels:
    app: pagerank-queuer 
spec:
  selector:
    matchLabels:
      app: pagerank-queuer 
  replicas: 1
  template:
    metadata:
      labels:
        app: pagerank-queuer 
    spec:
      containers:
      - name: pagerank-queuer
        imagePullPolicy: Always
        image: jinayshah7/pagerank-queuer:latest
        env:
          - name: SOURCE_TOPIC
            value: source_topic_name
          - name: DESTINATION_TOPIC
            value: destination_topic_name
        resources:
          limits:
            cpu: "1"
          requests:
            cpu: 50m
