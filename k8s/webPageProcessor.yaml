apiVersion: apps/v1
kind: Deployment
metadata:
  name: web-page-processor 
  labels:
    app: web-page-processor 
spec:
  selector:
    matchLabels:
      app: web-page-processor 
  replicas: 1
  template:
    metadata:
      labels:
        app: web-page-processor 
    spec:
      containers:
      - name: web-page-processor 
        imagePullPolicy: Always
        image: jinayshah7/web-page-processor:latest
        env:
          - name: SOURCE_TOPIC
            value: source_topic_name
          - name: DESTINATION_TOPIC
            value: destination_topic_name
        resources:
          limits:
            cpu: "1"
          requests:
            cpu: 50m
